<script
  src="{{ 'results-list.js' | asset_url }}"
  type="module"
  fetchpriority="low"
></script>

{% javascript %}
  const url = new URL(window.location.href);
  if (url.hash) {
    document.addEventListener(
      'DOMContentLoaded',
      () => {
        const card = document.getElementById(url.hash.slice(1));
        if (card) {
          card.scrollIntoView({ behavior: 'instant' });
        }
      },
      { once: true }
    );
  }
{% endjavascript %}

{% comment %} We always render this full-width, as the child blocks have width: page/full settings {% endcomment %}
<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<results-list
  class="section product-grid-container color-{{ section.settings.color_scheme }}"
  style="--padding-block-start: {{ section.settings.padding-block-start }}px; --padding-block-end: {{ section.settings.padding-block-end }}px;"
  section-id="{{ section.id }}"
  infinite-scroll="{{ section.settings.enable_infinite_scroll }}"
>
  {% render 'skip-to-content-link', href: '#ResultsList', text: 'accessibility.skip_to_results_list' %}

  <div
    class="collection-wrapper grid gap-style{% if section.settings.product_grid_width == 'full-width' %} collection-wrapper--grid-full-width{% endif %}"
  >
    {% content_for 'block',
      type: 'filters',
      id: 'filters',
      results: collection,
      results_size: collection.products_count
    %}

    {% assign products_per_page = 24 %}

    {% if section.settings.enable_infinite_scroll == false %}
      {% assign products_per_page = section.settings.products_per_page %}
    {% endif %}

    {% if section.settings.section_title != blank %}
      <h2 class="collection__section-title" style="grid-column: var(--centered);">
        {{ section.settings.section_title }}
      </h2>
    {% endif %}

    {% paginate collection.products by products_per_page %}
      {% capture children %}
        {% for product in collection.products %}
          {% if section.settings.promo_banner_enabled and forloop.index == section.settings.promo_banner_position %}
            <li class="product-grid__item product-grid__promo product-grid__promo--banner">
              {% render 'collection-promo-banner',
                image: section.settings.promo_banner_image,
                badge_text: section.settings.promo_banner_badge_text,
                heading: section.settings.promo_banner_heading,
                button_text: section.settings.promo_banner_button_text,
                button_url: section.settings.promo_banner_button_url
              %}
            </li>
          {% endif %}

          {% if section.settings.section_divider_enabled and forloop.index == section.settings.section_divider_position %}
            <li class="product-grid__section-divider">
              {% if section.settings.section_divider_text != blank %}
                <h2 class="collection__section-title collection__section-title--inline">
                  {{ section.settings.section_divider_text }}
                </h2>
              {% endif %}
            </li>
          {% endif %}

          {% if section.settings.promo_card_enabled and forloop.index == section.settings.promo_card_position %}
            <li class="product-grid__item product-grid__promo product-grid__promo--card">
              {% render 'collection-promo-card',
                image: section.settings.promo_card_image,
                subtitle: section.settings.promo_card_subtitle,
                heading: section.settings.promo_card_heading,
                body: section.settings.promo_card_body,
                button_text: section.settings.promo_card_button_text,
                button_url: section.settings.promo_card_button_url
              %}
            </li>
          {% endif %}

          <li
            id="{{ section.id }}-{{ product.id }}"
            class="product-grid__item product-grid__item--{{ forloop.index0 }}"
            data-page="{{ paginate.current_page }}"
            data-product-id="{{ product.id }}"
            ref="cards[]"
          >
            {% # theme-check-disable %}
            {% content_for 'block', type: '_product-card', id: 'product-card', closest.product: product %}
            {% # theme-check-enable %}
          </li>
        {% endfor %}
      {% endcapture %}

      {% render 'product-grid',
        section: section,
        children: children,
        products: collection.products,
        paginate: paginate,
        enable_infinite_scroll: section.settings.enable_infinite_scroll
      %}
    {% endpaginate %}
  </div>
</results-list>

{% stylesheet %}
  .collection__section-title {
    font-size: 36px;
    line-height: 47px;
    letter-spacing: -0.36px;
    font-weight: 400;
    color: var(--color-foreground);
    margin: 0;
    padding-block-start: 16px;
  }

  .collection__section-title--inline {
    padding-block-start: 0;
  }

  .product-grid__promo--banner {
    /* Figma promo banner = 2 cols (≈1023×495), same height as a
       product card. On mobile the @media block below switches to
       full-width since 2 cols ≈ full viewport there anyway. */
    grid-column: span 2;
    min-width: 0;
  }

  /* `dense` lets the next product backfill the leftover cell on row 2
     when the 2-col promo lands at col 1 (desktop col 3 / mobile col 2).
     Without dense, mobile leaves a visible gap because the full-width
     banner can't fit beside the 3rd product and gets pushed to its
     own row, stranding the cell. */
  .product-grid {
    grid-auto-flow: row dense;
  }

  .product-grid__section-divider {
    grid-column: 1 / -1;
    padding-block-start: 32px;
  }

  .product-grid__promo--card {
    min-width: 0;
  }

  /* Promo banner styles */
  .collection-promo-banner {
    position: relative;
    width: 100%;
    height: 100%;
    min-height: 495px;
    overflow: hidden;
    border-radius: var(--style-border-radius, 0);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .collection-promo-banner__media {
    position: absolute;
    inset: 0;
  }

  .collection-promo-banner__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .collection-promo-banner__content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
    text-align: center;
    padding: 24px;
    max-width: 670px;
  }

  .collection-promo-banner__badge {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 8px 24px;
    border: 1px solid #F2F7FB;
    border-radius: 50px;
    font-size: 18px;
    line-height: 26px;
    letter-spacing: 0.72px;
    text-transform: uppercase;
    color: #FFFFFF;
  }

  .collection-promo-banner__heading {
    font-size: 56px;
    line-height: 73px;
    letter-spacing: -1.12px;
    font-weight: 400;
    color: #FFFFFF;
    margin: 0;
  }

  .collection-promo-banner__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 10px 34px;
    background-color: #FFFFFF;
    color: #202635;
    border-radius: 57px;
    font-size: 16px;
    line-height: 23px;
    text-decoration: none;
    text-align: center;
    transition: opacity var(--animation-speed, 0.2s) var(--animation-easing, ease);
  }

  .collection-promo-banner__button:hover {
    opacity: 0.85;
  }

  /* Promo card styles */
  .collection-promo-card {
    position: relative;
    width: 100%;
    height: 100%;
    min-height: 495px;
    overflow: hidden;
    border-radius: var(--style-border-radius, 0);
    background-color: #202635;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .collection-promo-card__media {
    position: absolute;
    inset: 0;
  }

  .collection-promo-card__image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    opacity: 0.9;
  }

  .collection-promo-card__overlay {
    position: absolute;
    inset: 0;
    background-color: #202635;
    opacity: 0.7;
    mix-blend-mode: multiply;
  }

  .collection-promo-card__content {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 16px;
    text-align: center;
    padding: 24px;
    max-width: 430px;
  }

  .collection-promo-card__subtitle {
    font-family: monospace;
    font-size: 14px;
    line-height: 20px;
    letter-spacing: 0.56px;
    text-transform: uppercase;
    color: #FFFFFF;
  }

  .collection-promo-card__heading {
    font-size: 28px;
    line-height: 36px;
    letter-spacing: -0.28px;
    font-weight: 400;
    color: #FFFFFF;
    margin: 0;
  }

  .collection-promo-card__body {
    font-size: 18px;
    line-height: 25px;
    font-weight: 400;
    color: #FFFFFF;
    margin: 0;
  }

  .collection-promo-card__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 10px 34px;
    background-color: #FFFFFF;
    color: #202635;
    border-radius: 57px;
    font-size: 16px;
    line-height: 23px;
    text-decoration: none;
    text-align: center;
    transition: opacity var(--animation-speed, 0.2s) var(--animation-easing, ease);
  }

  .collection-promo-card__button:hover {
    opacity: 0.85;
  }

  @media screen and (max-width: 749px) {
    .collection__section-title {
      font-size: 22px;
      line-height: 29px;
      letter-spacing: -0.22px;
    }

    .product-grid__promo--banner {
      grid-column: 1 / -1;
    }

    .product-grid__section-divider {
      padding-block-start: 24px;
    }

    .collection-promo-banner {
      min-height: 300px;
    }

    .collection-promo-banner__badge {
      font-size: 13px;
      line-height: 18px;
      letter-spacing: 0.52px;
      padding: 6px 18px;
    }

    .collection-promo-banner__heading {
      font-size: 30px;
      line-height: 36px;
      letter-spacing: -0.6px;
    }

    .collection-promo-banner__button {
      padding: 10px 24px;
      font-size: 14px;
      line-height: 20px;
      border-radius: 60px;
    }

    .collection-promo-banner__content {
      gap: 6px;
      padding: 24px 12px;
    }

    .collection-promo-card {
      min-height: 388px;
    }

    .collection-promo-card__content {
      gap: 8px;
      padding: 24px 12px;
    }

    .collection-promo-card__subtitle {
      font-size: 10px;
      line-height: 14px;
      letter-spacing: 0.4px;
    }

    .collection-promo-card__heading {
      font-size: 20px;
      line-height: 24px;
      letter-spacing: -0.2px;
    }

    .collection-promo-card__body {
      font-size: 14px;
      line-height: 20px;
      letter-spacing: 0.14px;
    }

    .collection-promo-card__button {
      padding: 10px 24px;
      font-size: 14px;
      line-height: 20px;
      border-radius: 60px;
    }
  }

  /* Pill-style filter dropdowns (Figma horizontal filter look) */
  .facets--horizontal {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    justify-content: flex-end;
    align-items: center;
  }

  .facets--horizontal .facets__panel > .facets__summary {
    list-style: none;
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 18px;
    border: 1px solid var(--color-border);
    border-radius: 999px;
    background: var(--color-background);
    color: var(--color-foreground);
    cursor: pointer;
    font-size: 14px;
    line-height: 20px;
    white-space: nowrap;
  }

  .facets--horizontal .facets__panel > .facets__summary::-webkit-details-marker {
    display: none;
  }

  .facets--horizontal .facets__panel > .facets__summary::marker {
    display: none;
  }

  .facets--horizontal .facets__panel > .facets__summary:hover {
    background: rgba(0, 0, 0, 0.03);
  }

  .facets--horizontal details[open] > .facets__summary {
    border-color: var(--color-foreground);
  }

  /* Mobile drawer-toggle as full-width pill */
  @media screen and (max-width: 749px) {
    .facets-toggle__wrapper {
      width: 100%;
    }

    .facets-toggle__button {
      width: 100%;
      justify-content: space-between;
      padding: 12px 20px;
      border: 1px solid var(--color-border);
      border-radius: 999px;
      background: var(--color-background);
      font-size: 14px;
      line-height: 20px;
    }
  }

  /* Rounded corners on promo banner / card to match Figma */
  .collection-promo-banner,
  .collection-promo-card {
    border-radius: 24px;
  }

  @media screen and (max-width: 749px) {
    .collection-promo-banner,
    .collection-promo-card {
      border-radius: 16px;
    }
  }

  /* ============================================================
   * PLP alignment refinements (Figma)
   * ============================================================ */

  /* Section title sits ~32px below the toolbar divider on desktop */
  .collection__section-title {
    padding-block-start: 32px;
  }

  @media screen and (max-width: 749px) {
    .collection__section-title {
      padding-block-start: 20px;
    }
  }

  /* Inline section dividers ("Section Title" mid-grid).
     Figma desktop: ~120px breathing room above the title, ~60px below.
     With 32px grid row-gap + 32px on .product-grid__section-divider
     parent already accounted for, add 56px more on top and 32px below. */
  .collection__section-title--inline {
    padding-block-start: 56px;
    padding-block-end: 32px;
  }

  @media screen and (max-width: 749px) {
    .collection__section-title--inline {
      padding-block-start: 32px;
      padding-block-end: 16px;
    }
  }

  /* ============================================================
   * Filter drawer: Apply LEFT, Clear RIGHT (Figma button order)
   * ============================================================ */

  .facets__drawer-actions {
    flex-direction: row-reverse;
    align-items: center;
    gap: 15px;
  }

  /* Apply button (See Results) grows to fill leftover space */
  .facets__drawer-actions .facets__see-results {
    flex: 1 1 auto;
    min-width: 0;
    padding: 11px 24px;
    font-weight: 400;
  }

  /* Clear All sits content-width on the right */
  .facets__drawer-actions .facets__clear-all {
    flex: 0 0 auto;
    padding: 11px 24px;
    font-weight: 400;
  }

  /* ============================================================
   * Product cards (Figma 1849:1617): 20/26 medium title, light
   * reviews count, 24px image-to-stars gap
   * ============================================================ */

  /* Figma title: Fellix 20px/26px weight 500 letter-spacing -0.4px.
     Title renders via the `text` snippet inside <a ref="productTitleLink">
     wrapping <p role="heading"> with class .text-block.paragraph. */
  .product-grid__item a[ref="productTitleLink"] .text-block,
  .product-grid__item a[ref="productTitleLink"] [role="heading"] {
    font-weight: 500;
    letter-spacing: -0.4px;
    line-height: 26px;
  }

  /* Reviews count text: Figma weight 300 (light), 14/17 */
  .product-grid__item .rating-count {
    font-weight: 300;
    font-size: 14px;
    line-height: 17px;
  }

  /* Render "(N)" inline in drawer title instead of a separate badge */
  .facets-drawer__title {
    display: flex;
    align-items: baseline;
    gap: 4px;
  }

  .facets-drawer__title .facets__bubble {
    background: none;
    color: inherit;
    padding: 0;
    border-radius: 0;
    min-width: 0;
    width: auto;
    height: auto;
    font: inherit;
    border: none;
    display: inline;
  }

  .facets-drawer__title .facets__bubble::before {
    content: '(';
  }

  .facets-drawer__title .facets__bubble::after {
    content: ')';
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:names.collection_container",
  "enabled_on": {
    "templates": ["collection"]
  },
  "settings": [
    {
      "type": "select",
      "id": "layout_type",
      "label": "t:settings.type",
      "options": [
        {
          "value": "grid",
          "label": "t:options.grid"
        },
        {
          "value": "organic",
          "label": "t:options.editorial"
        }
      ],
      "default": "grid"
    },
    {
      "type": "select",
      "id": "product_card_size",
      "label": "t:settings.card_size",
      "options": [
        {
          "value": "small",
          "label": "t:options.small"
        },
        {
          "value": "medium",
          "label": "t:options.medium"
        },
        {
          "value": "large",
          "label": "t:options.large"
        },
        {
          "value": "extra-large",
          "label": "t:options.extra_large"
        }
      ],
      "default": "medium",
      "visible_if": "{{ section.settings.layout_type == 'grid' }}"
    },
    {
      "type": "select",
      "id": "mobile_product_card_size",
      "label": "t:settings.mobile_card_size",
      "options": [
        {
          "value": "small",
          "label": "t:options.small"
        },
        {
          "value": "large",
          "label": "t:options.large"
        }
      ],
      "default": "small"
    },
    {
      "type": "checkbox",
      "id": "enable_infinite_scroll",
      "label": "t:settings.auto_load_products",
      "default": true
    },
    {
      "type": "range",
      "id": "products_per_page",
      "label": "t:settings.products_per_page",
      "min": 8,
      "max": 36,
      "step": 4,
      "default": 24,
      "visible_if": "{{ section.settings.enable_infinite_scroll == false }}"
    },
    {
      "type": "header",
      "content": "t:content.layout"
    },
    {
      "type": "select",
      "id": "product_grid_width",
      "label": "t:settings.width",
      "options": [
        {
          "value": "centered",
          "label": "t:options.page"
        },
        {
          "value": "full-width",
          "label": "t:options.full"
        }
      ],
      "default": "centered"
    },
    {
      "type": "checkbox",
      "id": "full_width_on_mobile",
      "label": "t:settings.full_width_on_mobile",
      "default": true,
      "visible_if": "{{ section.settings.product_grid_width != 'full-width' }}"
    },
    {
      "type": "range",
      "id": "columns_gap_horizontal",
      "label": "t:settings.horizontal_gap",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "columns_gap_vertical",
      "label": "t:settings.vertical_gap",
      "min": 0,
      "max": 50,
      "step": 1,
      "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "padding-inline-start",
      "label": "t:settings.left_padding",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "range",
      "id": "padding-inline-end",
      "label": "t:settings.right_padding",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "header",
      "content": "t:content.section_layout"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:settings.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top_padding",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 8
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom_padding",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 8
    },
    {
      "type": "header",
      "content": "Section title"
    },
    {
      "type": "text",
      "id": "section_title",
      "label": "Section title"
    },
    {
      "type": "header",
      "content": "Promo banner"
    },
    {
      "type": "checkbox",
      "id": "promo_banner_enabled",
      "label": "Enable promo banner",
      "default": false
    },
    {
      "type": "number",
      "id": "promo_banner_position",
      "label": "Position in grid",
      "default": 4
    },
    {
      "type": "image_picker",
      "id": "promo_banner_image",
      "label": "Background image"
    },
    {
      "type": "text",
      "id": "promo_banner_badge_text",
      "label": "Badge text",
      "default": "LIMITED OFFER"
    },
    {
      "type": "text",
      "id": "promo_banner_heading",
      "label": "Heading",
      "default": "Subscribe and save 10% off replacement filters"
    },
    {
      "type": "text",
      "id": "promo_banner_button_text",
      "label": "Button text",
      "default": "Shop Now"
    },
    {
      "type": "url",
      "id": "promo_banner_button_url",
      "label": "Button link"
    },
    {
      "type": "header",
      "content": "Section divider"
    },
    {
      "type": "checkbox",
      "id": "section_divider_enabled",
      "label": "Enable section divider",
      "default": false
    },
    {
      "type": "number",
      "id": "section_divider_position",
      "label": "Position in grid",
      "default": 7
    },
    {
      "type": "text",
      "id": "section_divider_text",
      "label": "Divider title",
      "default": "Section Title"
    },
    {
      "type": "header",
      "content": "Promo card"
    },
    {
      "type": "checkbox",
      "id": "promo_card_enabled",
      "label": "Enable promo card",
      "default": false
    },
    {
      "type": "number",
      "id": "promo_card_position",
      "label": "Position in grid",
      "default": 11
    },
    {
      "type": "image_picker",
      "id": "promo_card_image",
      "label": "Background image"
    },
    {
      "type": "text",
      "id": "promo_card_subtitle",
      "label": "Subtitle",
      "default": "TITLE HERE"
    },
    {
      "type": "text",
      "id": "promo_card_heading",
      "label": "Heading"
    },
    {
      "type": "textarea",
      "id": "promo_card_body",
      "label": "Body text"
    },
    {
      "type": "text",
      "id": "promo_card_button_text",
      "label": "Button text",
      "default": "Take the Quiz"
    },
    {
      "type": "url",
      "id": "promo_card_button_url",
      "label": "Button link"
    }
  ],
  "presets": []
}
{% endschema %}
