<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div
  class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }}"
>
  {% liquid
    assign total_blocks = section.blocks.size

    # When first block spans full width, calculate grid based on remaining blocks
    assign effective_blocks = total_blocks
    if section.settings.first_block_full_width and total_blocks > 1
      assign effective_blocks = total_blocks | minus: 1
    endif

    # Grid layout calculations
    assign grid_columns = effective_blocks | at_most: 5

    # Calculate last row counts using modulo arithmetic
    # last_row_count = (effective_blocks - 1) % grid_columns + 1
    assign last_row_count = effective_blocks | minus: 1 | modulo: 5 | plus: 1
    assign tablet_last_row_count = effective_blocks | minus: 1 | modulo: 2 | plus: 1

    # Check if there are orphaned elements (single item in last row)
    assign has_isolated_grid_item_desktop = false
    assign has_isolated_grid_item_tablet = false

    if effective_blocks > 5 and last_row_count == 1
      assign has_isolated_grid_item_desktop = true
    endif

    if effective_blocks > 2 and tablet_last_row_count == 1
      assign has_isolated_grid_item_tablet = true
    endif
  %}
  <div
    class="
      footer-content
      spacing-style
      {% if section.settings.first_block_full_width %}footer-content--first-full-width{% endif %}
      {% if has_isolated_grid_item_desktop %}footer-content--isolated-grid-item-desktop{% endif %}
      {% if has_isolated_grid_item_tablet %}footer-content--isolated-grid-item-tablet{% endif %}
    "
    style="
      {%  render 'spacing-style', settings: section.settings %}
      --footer-gap: {{ section.settings.gap }}px;
      --grid-columns: {{ grid_columns }};
      --last-row-count: {{ last_row_count }};
      --tablet-last-row-count: {{ tablet_last_row_count }};
    "
  >
    {% content_for 'blocks' %}
  </div>
</div>

{% stylesheet %}
  .footer-content {
    contain: content;
    content-visibility: auto;
    display: grid;
    grid-template-columns: 1fr;
    gap: var(--footer-gap);
    align-items: start;
  }

  .footer-content .menu__heading__default {
    font-weight: var(--font-heading--weight);
  }

  @media screen and (min-width: 750px) and (max-width: 989px) {
    .footer-content {
      grid-template-columns: repeat(min(var(--grid-columns), 3), 1fr);
      grid-auto-flow: row;
    }

    .footer-content[style*='--grid-columns: 4'] {
      grid-template-columns: repeat(2, 1fr);
    }

    .footer-content[style*='--grid-columns: 5'] {
      grid-template-columns: repeat(2, 1fr);
    }

    .footer-content--first-full-width > :first-child {
      grid-column: 1 / -1;
    }

    .footer-content--isolated-grid-item-tablet > :last-child {
      grid-column: 1 / -1;
    }
  }

  @media screen and (min-width: 990px) {
    .footer-content {
      grid-template-columns: repeat(var(--grid-columns), 1fr);
    }

    /* Single item centered */
    .footer-content[style*='--grid-columns: 1'] {
      justify-items: center;
    }

    .footer-content--isolated-grid-item-desktop > :last-child {
      grid-column: 1 / -1;
    }

    /* First block spans full width on desktop */
    .footer-content--first-full-width > :first-child {
      grid-column: 1 / -1;
    }

    /* Desktop font-size overrides for newsletter text (28px) */
    .footer-content--first-full-width > :first-child .text-block.custom-font-size {
      --font-size: 1.75rem;
    }

    /* Desktop font-size overrides for email-signup input (28px) */
    .footer-content--first-full-width > :first-child .email-signup__input {
      font-size: 1.75rem;
    }
  }

  /* Override h5 preset uppercase on menu headings */
  .footer-content .menu__heading {
    text-transform: none;
  }

  /* Underline for link-styled buttons inside group blocks (quiz CTA) */
  .footer-content .group-block .link {
    text-decoration: underline;
  }

  @media screen and (max-width: 749px) {
    /* Hide quiz CTA card on mobile */
    .footer-content .group-block:has(.background-image-container) {
      display: none;
    }

    /* Mobile menu heading and item font sizes (Figma: 14px / line-height 20px) */
    .footer-content .menu__heading,
    .footer-content .menu__item {
      font-size: 0.875rem;
      line-height: 1.43;
    }
  }

  /* Desktop menu typography (Figma: heading 18px/25px, link 16px/22px) */
  @media screen and (min-width: 750px) {
    .footer-content .menu__heading {
      font-size: 1.125rem;
      line-height: 1.39;
    }

    .footer-content .menu__item {
      font-size: 1rem;
      line-height: 1.375;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:names.footer",
  "class": "section-wrapper",
  "blocks": [
    {
      "type": "_divider"
    },
    {
      "type": "@app"
    },
    {
      "type": "button"
    },
    {
      "type": "follow-on-shop"
    },
    {
      "type": "group"
    },
    {
      "type": "icon"
    },
    {
      "type": "image"
    },
    {
      "type": "menu"
    },
    {
      "type": "payment-icons"
    },
    {
      "type": "text"
    },
    {
      "type": "logo"
    },
    {
      "type": "jumbo-text"
    },
    {
      "type": "social-links"
    },
    {
      "type": "email-signup"
    }
  ],
  "enabled_on": {
    "groups": ["footer"]
  },
  "settings": [
    {
      "type": "checkbox",
      "id": "first_block_full_width",
      "label": "First block full width",
      "default": false
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "t:settings.width",
      "options": [
        {
          "value": "page-width",
          "label": "t:options.page"
        },
        {
          "value": "full-width",
          "label": "t:options.full"
        }
      ],
      "default": "page-width"
    },
    {
      "type": "range",
      "id": "gap",
      "label": "t:settings.gap",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 0
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:settings.color_scheme",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:content.padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "t:settings.top",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 20
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "t:settings.bottom",
      "min": 0,
      "max": 100,
      "step": 1,
      "unit": "px",
      "default": 20
    }
  ],
  "presets": [
    {
      "name": "t:names.footer",
      "category": "t:categories.footer",
      "settings": {
        "gap": 20,
        "color_scheme": "scheme-1",
        "padding-block-start": 36,
        "padding-block-end": 36
      },
      "blocks": {
        "newsletter-group": {
          "type": "group",
          "settings": {
            "content_direction": "column",
            "vertical_on_mobile": true,
            "horizontal_alignment": "flex-start",
            "vertical_alignment": "center",
            "gap": 6,
            "width": "fill",
            "inherit_color_scheme": true
          },
          "blocks": {
            "newsletter-heading": {
              "type": "text",
              "settings": {
                "text": "t:html_defaults.join_our_email_list",
                "width": "100%",
                "alignment": "left",
                "type_preset": "h3"
              }
            },
            "newsletter-text": {
              "type": "text",
              "settings": {
                "text": "t:html_defaults.get_exclusive_deals_and_early_access_to_new_products",
                "width": "100%",
                "alignment": "left",
                "type_preset": "rte"
              }
            }
          },
          "block_order": ["newsletter-heading", "newsletter-text"]
        },
        "newsletter-form": {
          "type": "email-signup",
          "settings": {
            "width": "fill"
          }
        }
      },
      "block_order": ["newsletter-group", "newsletter-form"]
    }
  ]
}
{% endschema %}
