## Consolidated

## Recent
[chat] Figma "Navigation" component instance fills are INVERTED vs the rendered design: node JSON shows nav text/logo as #FFFFFF (white) and the utility banner as #D9E8F4, but the actual render is a WHITE nav with dark #202635 text + blue logo, and a light-blue banner. Always sample pixels from the frame jpg (rendered image is ground truth), not the node `fills`.
[chat] Brand logo (ClearlyFiltered) is brand blue #3383C5 (= color scheme primary). The header nav uses scheme-1 (white bg #ffffff, foreground #202635); announcement bar uses scheme-7 (bg #D9E8F4, dark text); scheme-5 is dark navy (#202635 bg, white text); scheme-6 is transparent/white-text overlay.
[chat] A Figma logo that is a GROUP (not a single VECTOR) is NOT exported by `tf _figma-fetch`. Export it directly via the Figma images API: GET https://api.figma.com/v1/images/<fileKey>?ids=<instance-internal-node-id>&format=svg with Bearer token from ~/.theme-factory/figma-tokens.json (access_token). Instance-internal ids like `I1849:2605;4:179` work.
[chat] Recolor a Figma-exported SVG by replacing fill="white" with the brand hex, but SKIP fills inside <mask>...</mask> (luminance masks need white=visible). Track mask depth when doing the replace.
[chat] settings.logo is unset on this store, so the header falls back to shop-name text. blocks/_header-logo.liquid now inlines assets/logo-clearly-filtered.svg via inline_asset_content when settings.logo == blank (size via --header-logo-image-height / -mobile vars, width:auto). SVG logos can't use image_picker defaults (Shopify stores SVG as GenericFile).
[chat] header.liquid _header-menu block setting `menu_font_style` (values: inverse/inverse_large/none) controls SUBMENU SIZE, not text color — menu link color comes from the color scheme foreground. Don't change it to fix link color.
[chat] Desktop split-nav (3 links | centered logo | 3 links) is achieved in header.liquid via: logo_position=center + menu_position=center, then CSS absolutely centers .header-logo and uses .menu-list__list-item:nth-child(3){margin-inline-end:auto} + nth-child(4){margin-inline-start:auto}. Requires main-menu to have exactly 6 top-level items. Native search/account/cart actions render in the right grid column (far right).
[chat] CORRECTED: Horizon `blocks/filters.liquid` renders products-count TWICE — once at line 86 (in facets-controls-wrapper, gated by filter_style==vertical) AND once at line 244 (gated by filter_style==horizontal). So horizontal mode DOES output the count natively — adding another in main-collection.liquid duplicates it. Do not add a custom count wrapper; style the base block's `.products-count-wrapper` (and `.facets--horizontal .products-count-wrapper`) instead.
[chat] Wrapping the `{% content_for 'block', type: 'filters' %}` call inside a custom flex/grid div (e.g. .collection-toolbar) renders fine on the storefront — the filters block's own `--grid-column--desktop` self-positioning style becomes a no-op inside a flex parent, and the block content flows as a flex item.
[chat] To hide the native <details>/<summary> triangle on Horizon filter buttons, set `list-style: none` on `.facets__summary` AND zero out both `::-webkit-details-marker` and `::marker` (Firefox uses the latter). Then add your own caret/icon if needed.
[chat] templates/collection.json already wires inline PLP content via main-collection settings: promo_banner_* (Subscribe banner inserted at position 4), section_divider_* (extra "Section Title" at position 7), promo_card_* (Quiz card at position 11). The renderers live in main-collection.liquid + snippets/collection-promo-banner|card.liquid — toggle via the *_enabled checkbox + reposition via the *_position number setting.
[chat] sections/collection-hero.liquid is a custom section (not in base Horizon) producing the light-blue gradient hero with `gradient_color_start: #C1EFFA` → `gradient_color_end: #8DCEF9` at 20% opacity. Its h1 is 56px/73px desktop, 30px/36px mobile, with max-width:495px left-aligned content.
[chat] Horizon filter drawer already styles most of the Figma "Filter Expanded" mock — see blocks/filters.liquid stylesheet: section header `.facets--drawer .facets__summary` (uppercase 13px ls 0.52), blue chevron `.facets--drawer .facets__summary .icon-caret`, custom 20×20 checkbox with #202635 fill + white tick on `:checked`, drawer title 20px, .facets__see-results blue pill, .facets__clear-all outlined pill. Add the *deltas* (button order, parens count) from main-collection.liquid rather than touching the base block.
[chat] Filter drawer's bottom action row (.facets__drawer-actions) renders Clear All BEFORE See Results in the DOM and uses `justify-content: space-between` — i.e. Clear is on the LEFT, Apply on the RIGHT by default. Figma wants the opposite, so override with `flex-direction: row-reverse` + flex-grow on .facets__see-results. The Apply button label comes from `actions.see_items` translation ("See N items"), not "Apply Filters" — only the layout/style is fixable without rewriting the block.
[chat] The drawer title renders "Filter" + a separate `.facets__bubble` badge for the active count. To make it match Figma's inline "Filters (2)" text, restyle .facets-drawer__title .facets__bubble (transparent bg, inherited font, no border/padding) and add ::before { content: '(' } / ::after { content: ')' }.
[chat] Horizon's announcement bar (sections/header-announcements.liquid + blocks/_announcement.liquid) has NO native dismiss/close button. Figma shows a 12×12 X on the right; added a .announcement-bar__close button absolutely positioned (right: 16px desktop / 12px mobile) inside the <aside>, with an inline script that hides both the .announcement-bar and its previous-sibling .section-background div (the section-background is a sibling, not a child, so hiding only the <aside> leaves the colored strip visible).
[chat] Figma frame node 1849:2605 ("Navigation") and 1849:2748 ("Navigation Mobile") render in Figma image export as the announcement bar + an EMPTY white nav row beneath — the dynamic nav content (menu links, logo, action icons) does NOT appear in standalone exports. To audit the full nav design, look at the top portion of the full-page frames (e.g. PLP 1849:1617 desktop / 1849:1365 mobile), which include the rendered header at the top.
[chat] PLP "Page Hero" (sections/collection-hero.liquid) is a solid #F2F7FB band, NOT the gradient (#C1EFFA → #8DCEF9 at 20%) that an earlier iteration shipped. Heading "Shop All" is FT System 56px/73px ls -1.12 (30px/36px ls -0.6 mobile), description 18px/25px (14px/20px ls 0.14 mobile), color #202635. Padding: 64px top/bottom desktop, asymmetric 100px top / 24px bottom mobile (heavy top weighting is intentional per Figma).
[chat] PLP product card (Figma 1849:1617 grid items): 495×495 square image (image_ratio="square" in card-gallery), 24px gap to stars row. Stars: 5 × 14px-wide vector glyphs in brand blue #3383C5 followed by "2216 Reviews" in FT System 14/17 weight 300 (LIGHT). Title: Fellix 20/26 weight 500 letter-spacing -0.4px color #202635 (use `var(--font-heading--family)` + 1.25rem + letter_spacing:'tight' in block settings, then CSS refine to weight:500 and ls:-0.4px). Price: FT System 16/19 weight 400. Card width 495, gap 32 between cards (3 cols desktop).
[chat] product-title block renders <a ref="productTitleLink" class="contents user-select-text"> wrapping a <p role="heading" aria-level="3"> with .text-block.paragraph class (from snippets/text.liquid). Target CSS via `.product-grid__item a[ref="productTitleLink"] .text-block` or [role="heading"] — there is NO `.product-title` class.
[chat] PLP inline elements (promo banner / section divider / quiz card) positions in templates/collection.json refer to forloop.index — i.e. the element is inserted BEFORE the product at that index. With default grid-auto-flow, a `grid-column: 1 / -1` divider pushed mid-row leaves the prior row's last cell empty AND lets subsequent products backfill that cell, breaking the visual order.
[chat] CURRENT layout (per client preference, "3 products + promo as 4th, repeat in each section"): promo_banner=4, section_divider=8, promo_card=11. Banner spans 2 cols on desktop (`grid-column: span 2`), full-width on mobile. Section 1 grid: row 1 [P1,P2,P3], row 2 [BANNER cols1-2, P4 col3], row 3 [P5,P6,P7], DIVIDER full row, Section 2 row 1 [P8,P9,P10], row 2 [QUIZ col1, P11 col2, P12 col3], then more products. Added `grid-auto-flow: row dense` on .product-grid so the next product backfills the leftover cell — necessary on mobile where the full-width banner can't fit beside the 3rd product and gets pushed to its own row, otherwise leaving a visible gap.
[chat] Footer Figma spec (sections/footer.liquid + sections/footer-utilities.liquid, both scheme-5 dark navy #202635): newsletter row 28px/36px desktop, 20px/24px mobile (white, with #D3D8DD muted placeholder for email input). 4 menu columns Shop/Technology/Support/Company — heading 18px/25px desktop, 14px/20px mobile; item 16px/22px desktop, 14px/20px mobile. Quiz card "Find Your Filter" 20px/26px + "Lab Test Results" underlined 16px/23px — hidden on mobile via CSS. Bottom utilities row: copyright 14px/21px desktop, 10px/15px mobile; social-links block holds 3 icons (Instagram/Facebook/LinkedIn 20×20). `.utilities--blocks-2` already lays out copyright LEFT + social RIGHT via grid-template-columns:1fr 1fr + justify-self:end on nth-child(2).
[chat] Horizon `menu` block requires a linklist (navigation handle) that exists in Shopify admin. If you wire up `"menu": "shop"` in section JSON without creating the "shop" linklist in Online Store → Navigation, the column renders with the heading text only (or nothing). For shop themes where the merchant hasn't created those linklists, convert to a `group` block containing a `text` heading + N `button` blocks (style_class: "link", labels hardcoded). CSS to neutralize the button chrome: `.footer-content > .group-block .button { background:transparent; border:none; padding:0; color:var(--color-foreground); }`.
[chat] The Horizon `text` block renders its `font_size` setting as an INLINE style `style="--font-size: 1.25rem"`. Inline beats class-selector CSS at any specificity, so a media query that just sets `--font-size: 1.75rem` on `.text-block.custom-font-size` does NOT override. Fix is either (a) set the desktop value as the JSON `font_size` and override mobile with `!important`, or (b) put `!important` on the desktop override.
[chat] Figma header (1849:2605 desktop / 1849:2748 mobile) precise dimensions: Desktop nav row 80px (24px content + 28×28 padding), links FT System 17/24 w400, LSL/RSL gap 30px, logo 197×40 absolutely centered, page horizontal padding ~89-90px, action icons (search 20×20, account 21×21, cart 24×22). Mobile nav row 60px (29px logo + 15.5×15.5 padding), banner 56px (12/14 text wrapped to 2 lines, 14×14 padding), hamburger 20×15 at x=18 left, RSL = account 17×17 + cart 21×19 (NO search icon on mobile, hide via CSS). Horizon's default `--padding-sm` ≈ 16px on `.header__columns` is too small — override `--padding-block-start/end` directly on `.header__row--top .header__columns` to hit the 28px desktop spec.
